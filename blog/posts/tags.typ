// date: 2026-02-11
// tags: blog, tags


#let tag-link(name) = link("/tags/" + name + "/")[#name]
#set document(title: "Blog Tags")
#title()

This post serves as an elaboration on the tags used in this blog, as needed.

- #tag-link("ai-assisted"): Posts that were written with the help of AI tools, such as LLMs. This is distinct from the #tag-link("vibe-coding") tag which is when the content was produced with AI tools, but the post was written by hand.
